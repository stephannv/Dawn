<article class="single {{post_class}}">
    <header class="single-header kg-canvas">
        {{#is "post"}}
            <div class="single-meta">
                <span class="single-meta-item single-meta-date">
                    <time datetime="{{date format="YYYY-MM-DD"}}">
                        {{date published_at format="DD/MM/YYYY"}}
                    </time>
                </span>
                <span class="single-meta-item single-meta-length">
                    {{reading_time}}
                </span>
                {{#primary_tag}}
                    <span class="single-meta-item single-meta-tag">
                        <a class="post-tag post-tag-{{slug}}" href="{{url}}">
                            {{name}}
                        </a>
                    </span>
                {{/primary_tag}}
            </div>
        {{/is}}
        <h1 class="single-title">{{title}}</h1>
        {{#if custom_excerpt}}
            <div class="single-excerpt">
                {{custom_excerpt}}
            </div>
        {{/if}}
        {{#if feature_image}}
            {{#unless no_image}}
                <div class="single-media kg-width-{{width}} u-placeholder horizontal">
                    <img class="lazyload u-object-fit" data-srcset="{{> srcset}}"
                        data-sizes="auto" src="{{img_url feature_image size="l"}}"
                        srcset="data:image/gif;base64,R0lGODlhAQABAAAAACH5BAEKAAEALAAAAAABAAEAAAICTAEAOw=="
                        alt="{{title}}">
                </div>
            {{/unless}}
        {{/if}}
    </header>
    <div class="single-content kg-canvas u-text-format">
        {{#if @labs.members}}
            {{#if access}}
                {{content}}
                {{#is "post"}}
                    {{#unless @member}}
                        <section class="single-cta">
                            <h2 class="single-cta-title">Gostando do conteúdo? Torne-se um inscrito para mais conteúdos assim</h2>
                            <button class="button single-cta-button" data-portal="signup">Increver-se</button>
                            <br>
                            <button class="button-text single-cta-footer" data-portal="signin">Já é inscrito? Entre</button>
                        </section>
                    {{/unless}}
                {{/is}}
            {{else}}
                <p>
                    Você não tem acesso a esse artigo no momento, mas se você atualizar sua assinatura será capaz de acessar
                    todo o conteúdo, assim como todos os outros artigos já publicados.
                    Atualizando sua assinatura agora você terá acesso imediato.
                </p>
                <section class="single-cta">
                    {{#has visibility="paid"}}
                        <h2 class="single-cta-title">Esse artigo pode ser visto apenas pelos inscritos pagantes</h2>
                    {{else}}
                        <h2 class="single-cta-title">Esse artigo pode ser visto apenas pelos inscritos</h2>
                    {{/has}}

                    {{#if @member}}
                        {{#unless @member.paid}}
                            <button class="button single-cta-button" data-portal="account/plans">Atualize sua assinatura agora</button>
                        {{/unless}}
                    {{else}}
                        <button class="button single-cta-button" data-portal="signup">Seja inscrito</button>
                        <br>
                        <button class="button-text single-cta-footer" data-portal="signin">Já é inscrito? Entre</button>
                    {{/if}}
                </section>
            {{/if}}
        {{else}}
            {{content}}
        {{/if}}
    </div>
    {{#is "post"}}
        <footer class="single-footer container medium">
            <div class="single-footer-left">
                {{#prev_post}}
                    <div class="navigation navigation-previous">
                        <a class="navigation-link" href="{{url}}"
                            aria-label="Artigo anterior">
                            <svg class="icon navigation-icon">
                                <use xlink:href="#arrow-left"></use>
                            </svg>
                        </a>
                    </div>
                {{/prev_post}}
            </div>
            <div class="single-footer-middle">
                <div class="single-footer-top">
                    <h3 class="single-footer-title">Publicado por:</h3>
                    <div class="author-list">
                        {{#foreach authors}}
                            <div
                                class="author-image-placeholder u-placeholder square">
                                <a href="{{url}}" title="{{name}}">
                                    <img class="author-image lazyload u-object-fit"
                                        data-srcset="{{img_url profile_image size="xs"}}"
                                        src="{{img_url profile_image size="xs"}}"
                                        srcset="data:image/gif;base64,R0lGODlhAQABAAAAACH5BAEKAAEALAAAAAABAAEAAAICTAEAOw=="
                                        alt="{{name}}">
                                </a>
                            </div>
                        {{/foreach}}
                    </div>
                </div>
                <div class="single-footer-bottom">
                    <div class="share">
                        <a class="share-link share-link-facebook"
                            href="https://www.facebook.com/sharer.php?u={{url absolute="true"}}"
                            target="_blank" rel="noopener noreferrer">
                            <svg class="icon icon-facebook">
                                <use xlink:href="#facebook"></use>
                            </svg>
                            Compartilhe
                        </a>
                        <a class="share-link share-link-twitter"
                            href="https://twitter.com/intent/tweet?url={{url absolute="true"}}&text={{encode title}}"
                            target="_blank" rel="noopener noreferrer">
                            <svg class="icon icon-twitter">
                                <use xlink:href="#twitter"></use>
                            </svg>
                            Tweet
                        </a>
                    </div>
                </div>
            </div>
            <div class="single-footer-right">
                {{#next_post}}
                    <div class="navigation navigation-next">
                        <a class="navigation-link" href="{{url}}"
                            aria-label="Próximo artigo">
                            <svg class="icon navigation-icon">
                                <use xlink:href="#arrow-right"></use>
                            </svg>
                        </a>
                    </div>
                {{/next_post}}
            </div>
        </footer>
    {{/is}}
</article>
